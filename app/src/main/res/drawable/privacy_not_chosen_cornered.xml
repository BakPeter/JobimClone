<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">


    <!-- View background color -->
    <solid android:color="@color/terms_not_chosen_background"/>

    <stroke
        android:width="@dimen/terms_stroke_size"
        android:color="@color/colorPrimary" >
    </stroke>

    <!-- The radius makes the corners rounded -->
    <corners
        android:bottomLeftRadius="@dimen/terms_corners_radius"
        android:topLeftRadius="@dimen/terms_corners_radius" />

</shape>