<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">


    <!-- View background color -->
    <solid android:color="@color/taped_not_chosen_background"></solid>

    <!--     View border color and width-->
    <stroke
        android:width="@dimen/terms_stroke_size"
        android:color="@color/colorPrimary"></stroke>

    <!-- The radius makes the corners rounded -->
    <corners
        android:bottomRightRadius="@dimen/terms_corners_radius"
        android:topRightRadius="@dimen/terms_corners_radius" />

</shape>